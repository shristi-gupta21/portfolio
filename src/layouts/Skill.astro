---
const { imageName, heading } = Astro.props;
---

<div class="flex items-center justify-center gap-2">
  <img alt="" src=`/icons/${imageName}` class="h-6 md:h-10" />
  <span class="font-bold text-lg md:text-[28px] text-gray-800">{heading}</span>
</div>
